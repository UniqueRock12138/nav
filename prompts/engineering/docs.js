window.PROMPT_CONTENTS = window.PROMPT_CONTENTS || {};
Object.assign(PROMPT_CONTENTS, {
    "Sort-out-the-dialogue": `# 任务：生成独立可读的 Markdown 讨论纪要

请基于本次会话中实际出现的信息，将对话内容提炼、重组为一份结构清晰、具备独立可读性的 Markdown 文档。

目标：
- 让未参与本次讨论的人也能够理解讨论背景、核心问题、方案演进和最终结论。
- 保留有价值的信息，不遗漏关键技术细节。
- 避免流水账式记录，按照逻辑结构重新组织内容。

---

# 处理原则

## 1. 忠于事实，拒绝幻觉

仅基于本次会话中明确出现的信息进行总结。

禁止：
- 补充对话之外的背景信息；
- 推测未提及的需求、原因、目标；
- 添加行业经验、最佳实践或额外建议；
- 编造不存在的问题、方案、结论或行动项。

如果某信息无法从对话中确认，不要将其作为事实陈述。

---

## 2. 重组逻辑，而非简单整理聊天记录

不要按照聊天发生顺序逐条罗列。

应根据实际讨论内容进行归类，例如：
- 背景与目标；
- 架构设计；
- 技术方案；
- 问题分析；
- 方案对比；
- 问题排查过程；
- 最终决策。

分类标题必须来源于实际讨论内容。

禁止为了保持文档结构完整而创建未讨论的主题。

---

## 3. 保留方案演进过程

如果对话中存在方案调整、观点修正或错误纠正，应明确体现演进过程。

推荐：
初始思路 → 发现问题 → 最终调整

不存在明确变化时，不要强行构造。

---

## 4. 区分事实、分析和推断

区分：
- 对话明确出现的事实；
- 对话中的分析过程；
- 基于讨论得出的合理推论。

不要将模型自身推断包装成讨论结论。

---

## 5. 保留关键技术细节

对话中的命令、代码、配置、路径、版本号、错误信息、数据结构、专有名词必须准确保留。

使用 Markdown 对应格式展示。

---

## 6. 动态生成章节

章节根据实际内容生成。

不存在对应内容时：
- 直接省略章节；
- 不输出空标题；
- 不输出“暂无”“无相关内容”等占位描述。

---

# 输出结构

# [一句话概括核心主题] 讨论纪要

## 1. 背景与目标（如有）

## 2. 核心探讨内容（如有）

根据实际讨论拆分主题。

## 3. 方案演进与问题分析（如有）

仅记录明确发生的方案变化、问题发现和调整过程。

## 4. 关键结论与共识（如有）

总结最终确定方案、共识和核心知识点。

不确定信息标注：（待确认）

## 5. 遗留问题与 Action Items（如有）

仅记录：
- 未解决问题；
- 待确认事项；
- 未完成任务；
- 后续行动。

不存在时删除整个章节。

---

# 禁止行为

禁止：
1. 编造对话之外的信息；
2. 添加未讨论的技术方案；
3. 自动补充最佳实践；
4. 将简单问答包装成项目报告；
5. 为保持结构完整填充不存在的信息；
6. 输出模板占位内容；
7. 将模型推测描述为事实。

---

# 输出要求

- 只输出 Markdown 正文。
- 不输出前置说明或后置解释。
- 语言客观、精炼、专业。
- 删除聊天寒暄、重复表达和无价值内容。
`,

    "Extract-the-conclusion": `# Role
你是一位知识库编辑，专长是从会议纪要类文档中提炼可长期复用的结论性条目。

# Task
输入是一份由"对话复盘提示词"产出的 Markdown 纪要文档（包含背景、探讨过程、结论、遗留问题等模块）。
你的任务是从该纪要中**抽取所有确定性、可复用的结论**，重组为一份用于个人归档与检索的知识条目文档。

# Extraction Rules（提取规则）
**必须保留**：
- 已达成共识的事实性结论（如某 API 的行为、某概念的定义、某参数的取值）
- 已验证的经验/做法（如某场景下的最佳处理方式、踩坑后的修正方案）
- 已拍板的决策及其判定依据（仅保留"决策本身 + 关键依据"，不保留讨论过程）
- 可独立成立的知识点（脱离原对话上下文仍能读懂）

**必须丢弃**：
- 标注"待确认"、"存疑"、"未达共识"的内容
- 方案演进过程（"初始思路 → 发现问题 → 最终修正"中的前两段，只留最终结论）
- 背景介绍、目标陈述、Action Items、遗留问题
- 仅适用于一次性场景、无复用价值的临时性结论
- 任何过程性、过渡性、修饰性表述

**禁止行为**：
- 不得补充原纪要中未出现的信息（包括看似显然的领域常识）
- 不得合并意义不同的多个结论
- 不得改写原文中的代码、命令、参数值、专有名词

# Output Format`,

    "document-mergence": `优化文档，要求如下：【
# 任务目标
整合并优化多文件知识文档为高质量 Markdown

## 处理流程

### 步骤 1：文件整合决策
1. 文件盘点 — 列出各文件的主题、核心要点及互相引用/重复处
2. 决策规则：
   - 高度相关且互补 → 合并为单一文档，并给出章节草案
   - 主题或受众不同 → 保持分文件，列出建议文件名与摘要
   - 部分内容重叠 → 抽取共通部分做"基础文档"，其余分别独立

### 步骤 2：内容优化
针对合并后或各独立文档：

1. 准确性与逻辑 — 核实事实、纠正错误，确保论证链完整
2. 通俗化处理：
   - 为专业术语加简明解释（括号/短语）
   - 用具体示例阐释抽象概念
   - 面向非专业读者确保易懂
3. 精简与结构 — 删除冗余、保留核心信息，优化段落顺序与过渡

## 输出要求
1. 内容质量：保留核心观点，事实准确，逻辑严谨
2. 结构形式：标题层次分明，段落适中，Markdown 输出
3. 语言风格：简洁专业，适配非专业读者
4. 文件输出：生成完整 ".md" 文件（单一或多个），可直接下载

## 必须附带的说明
1. 所选整合方案 & 决策理由
2. 主要修改点列表（列出关键修订、增删与结构调整）
】`,

    "document-filing": `优化文档，要求如下：【
# 任务目标
把杂乱/复杂文档优化为清晰、逻辑严谨的高质量 Markdown 文档

## 处理流程
### 步骤 1：主题分析
1. 识别核心主题，提炼关键概念，确认内容是否聚焦
2. 内容取舍：
   - 偏离主题的判断保留（补充价值）或删除
   - 查缺补漏记录缺失的关键信息点

### 步骤 2：内容优化
1. 准确性与逻辑：
   - 核实数据与概念，纠正错误
   - 衔接论证链，避免逻辑跳跃
2. 通俗化与可读性：
    - 专业术语加简明解释（括号/短语）
    - 通过案例、类比阐释抽象概念
    - 面向非专业读者确保易懂
3. 结构与表达：
    - 删除重复、精简冗余
    - 调整段落顺序与过渡
    - 用明确、简洁的语言

## 输出要求
1. 内容质量：保留核心观点，事实准确，逻辑严谨
2. 结构形式：标题层次分明，段落长度适中，Markdown 输出
3. 语言风格：简洁专业，适配非专业读者，代码示例精炼
4. 附加说明：生成完整 ".md" 文件，供直接下载或复制

## 必须附带的说明
1. 步骤 1 结果：主题分析与取舍理由
2. 步骤 2 结果：具体优化措施与改进要点
3. 优化效果总结：优化前后对比，突出主要改进
】`,
});
